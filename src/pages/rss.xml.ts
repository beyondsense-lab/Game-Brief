import type { APIRoute } from 'astro'; import { env } from 'cloudflare:workers'; import { getPublishedArticles } from '@/db/queries/public';
export const GET:APIRoute=async(ctx)=>{const base=env.PUBLIC_SITE_URL; const items=(await getPublishedArticles(ctx as any,50)).map((a:any)=>`<item><title><![CDATA[${a.title}]]></title><link>${base}/news/${a.slug}</link><description><![CDATA[${a.excerpt}]]></description><pubDate>${new Date(a.published_at||a.created_at).toUTCString()}</pubDate></item>`).join(''); return new Response(`<?xml version="1.0"?><rss version="2.0"><channel><title>GAME//BRIEF</title><link>${base}</link><description>The gaming news worth knowing.</description>${items}</channel></rss>`,{headers:{'content-type':'application/rss+xml'}})};
