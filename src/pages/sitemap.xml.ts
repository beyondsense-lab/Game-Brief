import type { APIRoute } from 'astro'; import { getPublishedArticles,getGames } from '@/db/queries/public';
export const GET:APIRoute=async(ctx)=>{const base=ctx.locals.runtime.env.PUBLIC_SITE_URL; const articles=await getPublishedArticles(ctx as any,1000); const games=await getGames(ctx as any,1000); const urls=['/','/news','/reviews','/guides','/games','/newsletter','/releases',...articles.map((a:any)=>`/news/${a.slug}`),...games.map((g:any)=>`/games/${g.slug}`)].map(p=>`<url><loc>${base}${p}</loc></url>`).join(''); return new Response(`<?xml version="1.0"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls}</urlset>`,{headers:{'content-type':'application/xml'}})};
