import { now, readingTime, slugify } from '@/lib/utils/slug';
export async function adminAll(ctx:any, table:string){return (await ctx.locals.runtime.env.DB.prepare(`SELECT * FROM ${table} ORDER BY updated_at DESC, created_at DESC`).all()).results||[]}
export async function dashboardStats(ctx:any){const db=ctx.locals.runtime.env.DB; const names=['articles','games','reviews','guides','newsletter_subscribers','newsletter_issues']; const out:any={}; for(const n of names){out[n]=(await db.prepare(`SELECT count(*) c FROM ${n}`).first<any>())?.c||0} for(const s of ['draft','published','scheduled']) out[s]=(await db.prepare('SELECT count(*) c FROM articles WHERE status=?').bind(s).first<any>())?.c||0; return out}
export async function saveArticle(ctx:any,form:FormData,id?:string){const db=ctx.locals.runtime.env.DB, n=now(); const title=String(form.get('title')||'Untitled Brief'); const data=[title,String(form.get('slug')||slugify(title)),String(form.get('excerpt')||''),String(form.get('content')||''),String(form.get('heroImage')||''),String(form.get('heroImageAlt')||''),Number(form.get('authorId')||1),Number(form.get('categoryId')||1),Number(form.get('gameId')||0)||null,String(form.get('status')||'draft'),String(form.get('publishedAt')||''),String(form.get('scheduledAt')||''),readingTime(String(form.get('content')||'')),form.get('featured')==='on'?1:0,form.get('trending')==='on'?1:0,String(form.get('seoTitle')||title),String(form.get('seoDescription')||form.get('excerpt')||''),String(form.get('canonicalUrl')||''),String(form.get('ogImage')||''),form.get('allowIndexing')==='on'?1:0]; if(id){await db.prepare(`UPDATE articles SET title=?,slug=?,excerpt=?,content=?,hero_image=?,hero_image_alt=?,author_id=?,category_id=?,game_id=?,status=?,published_at=?,scheduled_at=?,reading_time=?,featured=?,trending=?,seo_title=?,seo_description=?,canonical_url=?,og_image=?,allow_indexing=?,updated_at=? WHERE id=?`).bind(...data,n,id).run(); return id} const r=await db.prepare(`INSERT INTO articles(title,slug,excerpt,content,hero_image,hero_image_alt,author_id,category_id,game_id,status,published_at,scheduled_at,reading_time,featured,trending,seo_title,seo_description,canonical_url,og_image,allow_indexing,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(...data,n,n).run(); return r.meta.last_row_id}
